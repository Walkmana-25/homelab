<task id="ses_0f773c3a0ffeyaI1fYYXaPKYTB" state="completed">
<task_result>
Report written to `.opencode/exploration/ansible-k3s-structure.md` (408 lines). Structured return below.

**summary:**
Explored the full `ansible/` tree plus the pinned `k3s-io/k3s-ansible` collection (installed as FQCN `k3s.orchestration` at `~/.ansible/collections/ansible_collections/k3s/orchestration/`, SHA `2c3f3773...`). For Harbor planning, the headline finding is that the collection **natively supports `registries_config_yaml`** — its `prereq` role writes the var verbatim to `/etc/rancher/k3s/registries.yaml` on every node (server + agent) before install. No existing registries/containerd/mirror config exists in `ansible/`; no ansible-vault is used anywhere (`group_vars`/`host_vars` are empty, `token: "changeme!"` is plaintext); `site.yml` is just a ping stub and real orchestration lives in the `Makefile`. The repo's templating idiom is `ansible.builtin.template: src: X.j2 ... notify: Restart` + handler.

**key_findings:**
- **k3s inventory** (`ansible/inventories/k3s/inventory.yml`, 58 lines): group `k3s_cluster` → children `server` (1 node) + `agent` (2 nodes). The "server flags" section is `extra_server_args` (folded scalar, NOT `server_flags`): `--disable traefik/local-storage/servicelb --flannel-backend=host-gw`. The manifests/registries block (lines 42–58) is all **commented out** — `extra_manifests`, `server_config_yaml`, `agent_config_yaml`, `registries_config_yaml` are documented but unused.
- **`registries_config_yaml` mechanism** (collection `roles/prereq/tasks/main.yml` lines 321–333): `when: registries_config_yaml is defined` → mkdir `/etc/rancher/k3s` → `copy: content: "{{ registries_config_yaml }}" dest: "/etc/rancher/k3s/registries.yaml" mode: "0644"`. Applies to ALL `k3s_cluster` hosts. Same role handles `extra_manifests` → `/var/lib/rancher/k3s/server/manifests/`.
- **Collection invocation**: `make install-k3s` runs `k3s.orchestration.site` (prereq→airgap→raspberrypi, then k3s_server, then k3s_agent). Ordered pipeline: nfs-client → btrfs-longhorn → `k3s.orchestration.site` → (same, `--tags kubeconfig`) → fetch-kubeconfig → argocd. Do not reorder.
- **No secrets in ansible**: vault grep returns only a comment; in-cluster secrets use SealedSecrets (per AGENTS.md). So Harbor admin/DB passwords should be SealedSecrets, not ansible vault.
- **Config-deploy idioms**: (A) `roles/<r>/templates/X.j2` + `notify: Restart` + handler (fluent_bit, node_exporter); (B) inline `copy: content: |` (router netplan, and the collection's registries/config.yaml). NFS role uses `lineinfile` for `/etc/exports`. No `.j2` writes to `/etc/rancher/k3s/` today. NFS exports `/srv/nfs/k3s` to `10.2.0.0/24` with `rw,sync,no_subtree_check,no_root_squash` on btrfs `/dev/vda`.
- **Lint**: `.ansible-lint` = default profile, excludes `.git/.github` only. New tasks must use FQCN (`ansible.builtin.*`, `ansible.posix.*`, `community.general.*`, `kubernetes.core.*`), per-task `become: true`, quoted `mode: "0644"`, and a handler for any restart.

**artifact_path:** `/home/ubuntu/homelab/.opencode/exploration/ansible-k3s-structure.md`
</task_result>
</task>