apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sealed-secrets
  - cloudnative-pg
  - cert-manager
  - metallb
  - metallb-config
  - nfs-provisioner
  # Decommissioned 2026-08-22: all PVCs migrated to ssd (LINSTOR) / nfs.
  # Manifest kept in repo for reference; re-enable to reinstall.
  # - longhorn
  - cloudflare-tunnel-ingress-controller
  - victoria-metrics
  - grafana
  - headlamp
  - harbor
  - argocd-ingress
  - loki
  - fluent-bit
  - piraeus
