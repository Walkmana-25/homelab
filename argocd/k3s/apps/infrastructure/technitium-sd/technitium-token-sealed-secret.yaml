# PLACEHOLDER — intentionally comment-only so `kustomize build` keeps working.
#
# Generate the real SealedSecret with kubeseal (see README.md for details):
#
#   kubectl create secret generic technitium-sd-token \
#     --from-literal=token=<API_TOKEN> \
#     -n monitoring \
#     --dry-run=client -o yaml | \
#     kubeseal --controller-namespace kube-system -o yaml \
#     > argocd/k3s/apps/infrastructure/technitium-sd/technitium-token-sealed-secret.yaml
#
# Then add "- technitium-token-sealed-secret.yaml" to the resources list in
# kustomization.yaml and let ArgoCD sync. Do NOT commit the plaintext secret.
