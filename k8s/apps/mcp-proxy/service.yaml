apiVersion: v1
kind: Service
metadata:
  name: mcp-proxy
  labels:
    app.kubernetes.io/name: mcp-proxy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-proxy
  ports:
    - port: 9090
      targetPort: 8080
      name: http
