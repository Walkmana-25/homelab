apiVersion: v1
kind: Service
metadata:
  name: searxng-mcp
  labels:
    app.kubernetes.io/name: searxng-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: searxng-mcp
  ports:
    - port: 3000
      targetPort: 3000
      name: http
