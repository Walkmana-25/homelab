apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drive-data
  namespace: simpleapp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 1Gi
